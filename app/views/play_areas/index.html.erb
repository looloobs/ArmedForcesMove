<h1>Listing play_areas</h1>

<table>
  <tr>
  </tr>

<% @play_areas.each do |play_area| %>
  <tr>
    <td><%= link_to 'Show', play_area %></td>
    <td><%= link_to 'Edit', edit_play_area_path(play_area) %></td>
    <td><%= link_to 'Destroy', play_area, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New play_area', new_play_area_path %>